const notas = [10, 6, 8]

notas.push (7, 4); //adiciona elementos no final do array

console.log(notas);

const media = (notas[0] + notas[1] + notas[2] + notas[3] + notas[4]) / notas.length;

console.log(media)